{% extends 'base.html.twig' %}

{% block title %}Prestations{% endblock %}

{% block body %}
    <h1>Prestations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prestation.id }}</td>
            </tr>
              <tr>
                <th>libelle</th>
                <td>{{ prestation.libelle }}</td>
            </tr>
              <tr>
                <th>description</th>
                <td>{{ prestation.description }}</td>
            </tr>
            <tr>
                <th>prix</th>
                <td>{{ prestation.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_prestation') }}">back to list</a>

    <a href="{{ path('app_admin_prestation_edit', {'id': prestation.id}) }}">edit</a>

    {{ include('admin_prestation/_delete_form.html.twig') }}
{% endblock %}
